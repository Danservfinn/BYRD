# SearXNG settings for BYRD
# Minimal config - let SearXNG use defaults for most settings

use_default_settings: true

general:
  instance_name: "BYRD Search"

server:
  port: 7860
  bind_address: "0.0.0.0"
  secret_key: "byrd-searxng-secret-key-2024"
  limiter: false

search:
  safe_search: 0
  default_lang: "en"
  formats:
    - html
    - json

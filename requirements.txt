# BYRD Dependencies

# Neo4j driver for memory
neo4j>=5.0.0

# HTTP client for LLM and search
httpx>=0.25.0

# DuckDuckGo search (primary search engine)
# Note: Package was renamed from duckduckgo-search to ddgs
ddgs>=6.0.0

# Claude API for Actor
anthropic>=0.39.0

# Config parsing
pyyaml>=6.0

# Environment variable loading
python-dotenv>=1.0.0

# SSL certificates (required for Neo4j Aura on macOS)
certifi>=2024.0.0

# FastAPI server for visualization UI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# NumPy for graph algorithms
numpy>=1.24.0

# System monitoring for compute introspection (optional, graceful fallback)
psutil>=5.9.0

# Optional: for embeddings (future)
# sentence-transformers>=2.2.0
